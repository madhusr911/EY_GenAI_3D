# D3 · M3.3 — Multi-Agent Patterns

**Duration:** ~1.5 hrs · **Type:** Individual, run-and-understand

## The situation

M3.1 built one agent with two tools. Today those two tools each become their own small
specialist agent, and a supervisor decides which specialist(s) a question needs, hands each one
a focused instruction, and combines their answers.

This notebook is fully working code — nothing to write. Run each cell, and read what it prints.

## Work this side by side with the concept page, not start to finish

- `Day3/concept/D3_M3.3_MultiAgent_Patterns_Concept.html` — five short sections (Foundation, A,
  B, C, D+E)
- `Day3/labs/starter/D3_M3.3_MultiAgent_Patterns_Starter.ipynb` — the notebook below

Read a concept section on the page → run the matching task → come back for the next concept.
Four round trips, not one long read followed by one long lab.

## Prerequisites

- Day 3 M3.1 completed — this lab reuses its exact calculator and `search_meridian_kb` tools.
- `OPENAI_API_KEY` set as an environment variable (same `.env` file all week).
- No new packages — same `langchain` / `langchain-openai` as M3.1.

## How to work through this module

1. Open the concept page and the notebook side by side.
2. Run **Setup** first.
3. **Foundation + Concept A → Task 1.** Build both specialists, test each alone.
4. **Concept B → Task 2.** Build the supervisor's routing + hand-off. Test a calculator-only,
   a research-only, and a needs-both question.
5. **Concept C → Task 3.** Confirm neither specialist's hand-off mentions the other's topic.
6. **Concept D + Concept E → Task 4.** Run the needs-both question sequentially, then in
   parallel, and time both.
7. Read the Recap, then confirm the notebook's final cell has run.

## What each task shows you

| Task | What it builds | Concept it matches |
|---|---|---|
| 1 | Calculation Specialist and Research Specialist, each tested alone | A — The Supervisor |
| 2 | Supervisor routing + hand-off, tested on 3 question types | B — Hand-offs |
| 3 | Proof that each hand-off is isolated from the other's topic | C — Shared vs Isolated State |
| 4 | Sequential vs parallel timing on the same needs-both question | D — What It Costs, E — Run Them Together |
| 5 | Results saved | — |

## What's next

M3.4 hardens a Day 3 agent for real production use — memory, guardrails, and a
deployment-readiness checklist.

## Expected Output

- Both specialists working correctly on their own (Task 1).
- The supervisor correctly routing all three question types, with the right specialist(s)
  called each time (Task 2).
- Confirmed, inspected proof that each specialist's hand-off never mentions the other's topic
  (Task 3).
- A real timing comparison showing parallel is faster than sequential, same answers either way
  (Task 4).
- `m3_3_multiagent_results.json` saved next to the notebook (Task 5).

## Submission Checklist

- [ ] Worked through all sections (Foundation + A-E) and their matching notebook tasks, in order
- [ ] Can explain, in your own words, what a supervisor does and doesn't do
- [ ] Task 2's needs-both question correctly called BOTH specialists and combined their answers
- [ ] Task 3's two `assert` lines passed
- [ ] Can explain, using Task 4's own numbers, why parallel was faster
- [ ] Notebook's final cell run — `m3_3_multiagent_results.json` saved

---
*Student-facing document. No trainer notes, answer keys, or rubric details belong in this file.*
